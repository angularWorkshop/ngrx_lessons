# Написать contract-тест, который гарантирует правильный action sequence для submit exercise flow.

Answer branch for the EduTec NgRx course.

## Topic

Тестирование action contracts и store facade

## Task

Написать contract-тест, который гарантирует правильный action sequence для submit exercise flow.

## What to finish

- Open `src/index.ts`.
- Complete `completeNgrxExercise()`.
- Keep the expected artifacts: event action, pure reducer, immutable state update.
- Save the file and wait for the StackBlitz status runner.

## Checks

`npm run check` runs TypeScript and Vitest checks.

Expected result for a completed solution: `ASSIGNMENT STATUS: COMPLETED`.
