# Переписать простой ComponentStore-сценарий на SignalStore и сравнить читаемость.

Answer branch for the EduTec NgRx course.

## Topic

SignalStore basics: withState, withComputed, withMethods

## Task

Переписать простой ComponentStore-сценарий на SignalStore и сравнить читаемость.

## What to finish

- Open `src/index.ts`.
- Complete `completeNgrxExercise()`.
- Keep the expected artifacts: local state, updater, component effect.
- Save the file and wait for the StackBlitz status runner.

## Checks

`npm run check` runs TypeScript and Vitest checks.

Expected result for a completed solution: `ASSIGNMENT STATUS: COMPLETED`.
